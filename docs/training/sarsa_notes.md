Try GNN embedding nodes and edges (maybe global). 
Sample might not be full story. 
Add some perturbation to us?

Add some memory? 
Temporal Bound -> Euclidean Space -> Spatial Bound (Self Adaptive?)

How do we want to use memory?

Problem with taking action from the sate right now is we don't know where the enemy will be in new state.
We take difference between two states? We need to know direction. Gradient decent/ascent. Movement can be generalized to other characteristics. NO Absolute value.

Use a neural network to estimate Q value?

S_t and Memory? -> NN -> Q(S_t, A_t)

You can use same nn
Transformer

---

Exploration vs exploitation
Monte Carlo Method

Bayes Inference